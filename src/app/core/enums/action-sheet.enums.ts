export enum ActionEnum {
  Add = 'add',
  Edit = 'edit',
  Delete = 'delete',
  Cancel = 'cancel',
  Confirm = 'confirm',
}

export enum ActionTextEnum {
  Add = 'Add',
  Edit = 'Edit',
  Delete = 'Delete',
  Cancel = 'Cancel',

}

export enum ActionRoleEnum {
  Cancel = 'cancel',
  Destructive = 'destructive',
}
