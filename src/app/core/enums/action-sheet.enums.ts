export enum ActionsEnum {
  Add = 'Add',
  Edit = 'Edit',
  Delete = 'Delete',
  Cancel = 'Cancel',
  Confirm = 'Confirm',
}

export enum ActionsRoleEnum {
  Cancel = 'cancel',
  Destructive = 'destructive',
}
